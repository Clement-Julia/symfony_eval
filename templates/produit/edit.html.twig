{% extends 'base.html.twig' %}

{% block title %}{{ 'produit.modification'|trans }}{% endblock %}

{% block body %}
    <a class="btn btn-outline-primary" href="{{ path('produit_index') }}">{{ 'retour'|trans }}</a>

    <h1 class="text-center display-5">{{ 'produit.modification'|trans }}</h1>

    {{ include('produit/_form.html.twig', {'button_label': 'Update'}) }}
    {{ include('produit/_delete_form.html.twig') }}


{% endblock %}
