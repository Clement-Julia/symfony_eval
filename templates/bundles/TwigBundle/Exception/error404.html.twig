{% extends 'base.html.twig' %}

{% block title %} {{ 'error.page_404'|trans }}{% endblock %}

{% block body %}
<div class="d-flex align-items-center justify-content-center mt-mid">
    <div class="text-center">
        <h1 class="display-1 fw-bold">{{ 'error.404'|trans() }}</h1>
        <p class="fs-3"> <span class="text-danger">{{ 'error.oups'|trans() }}</span> {{ 'error.page_non_trouve'|trans() }}</p>
        <p class="lead">
            {{ 'error.page_non_trouve_exp'|trans() }}
          </p>
        <a href="{{ path('produit_index') }}" class="btn btn-primary">{{ 'error.home'|trans() }}</a>
    </div>
</div>
{% endblock %}